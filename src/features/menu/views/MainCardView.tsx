import { Box } from "@mui/material";

const MainCardView = () => {
  return (
    <div
      style={{
        height: "600px",
        backgroundColor: "green",
        width: "100%",
        display: "flex",
        justifyContent: "center",
        alignItems: "center",
        position: "absolute",
        top: "1600px",
      }}
    >
      <Box></Box>
    </div>
  );
};

export default MainCardView;
