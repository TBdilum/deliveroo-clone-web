import { Box } from "@mui/material";
import React from "react";

const PopularView = () => {
  return <Box sx={{ background: "yellow", height: "300px" }}>PopularView</Box>;
};

export default PopularView;
